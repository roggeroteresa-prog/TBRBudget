# Secrets
.env

# Node
node_modules/
frontend/dist/

# Python
__pycache__/
*.pyc
.venv/
venv/

# ChromaDB persistenza locale (se usata su disco invece che via server)
chroma_data/

# Grafici generati a runtime (tenere solo .gitkeep)
data_agent/charts/*.png

# Dati runtime generati durante l'uso (budget/righe creati e storico azioni):
# si rigenerano da soli vuoti al primo avvio, non hanno senso nel repository.
# users-store.json invece resta tracciato: contiene solo l'utente admin di
# seed, necessario perché l'app abbia un utente predefinito al primo avvio.
# backend/src/data/budgets-store.json
# backend/src/data/history-log.json

# OS
.DS_Store